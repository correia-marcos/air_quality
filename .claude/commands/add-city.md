---
description: Register a new city through the registry pattern (not by copy-pasting scripts)
argument-hint: '[city_id]  e.g. lima'
---

Read `doc/ai/workflows/add-city.md` and the applicable shared rules.
Interpret `$ARGUMENTS` as the named inputs described there. Preserve task scope.
